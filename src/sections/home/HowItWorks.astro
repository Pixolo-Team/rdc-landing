---
/** Data for "How It Works" */
const sectionContent = {
  title: "How It Works",
  boxItemList: [
    {
      icon: "/images/resume-logo.svg",
      alt: "Resume",
      text: "Register and lock your seat at select Reliance Digital centers",
    },
    {
      icon: "/images/ticket-logo.svg",
      alt: "Ticket",
      text: "Receive your ticket + Masterclass Confirmation Number by email",
    },
    {
      icon: "/images/coupon-logo.svg",
      alt: "Coupon",
      text: "Receive two Reliance Digital Referral Discount codes by email",
      subText: "(terms and validity included)",
    },
    {
      icon: "/images/certificate-logo.svg",
      alt: "Certificate",
      text: "Attend, build with Microsoft Copilot, and get certified",
    },
  ],
};
---

<section class="container mx-auto w-full px-4 py-16 sm:px-10">
  <div class="flex flex-col justify-center gap-9 xl:gap-6 items-center">
    <!-- Section Title Header -->
    <h2
      class="section-title xl:w-3/5"
      data-sal="slide-up"
      data-sal-delay="200"
      data-sal-duration="300"
    >
      {sectionContent.title}
    </h2>

    <!-- Box Item list -->
    <div class="flex flex-col xl:flex-row gap-4 w-full">
      {
        sectionContent.boxItemList.map((boxItem) => (
          <div
            class="flex flex-col gap-6 items-center justify-center w-full md:flex-1 bg-n-50 border-2 border-n-800 
                   hover:shadow-lg transition-shadow duration-300 px-12 py-16 md:py-12 md:px-10"
            data-sal="slide-up"
            data-sal-duration="400"
            data-sal-easing="ease-out-back"
          >
            {/* Logo */}
            <img src={boxItem.icon} alt={boxItem.alt} class="h-14 w-auto" />
            <div class="w-full sm:w-2/3 md:w-3/4 lg:w-full">
              {/* Main Text */}
              <p class="text-center text-n-900 text-2xl font-medium leading-snug">
                {boxItem.text}
              </p>
              {/* Subtext optional */}
              {boxItem.subText && (
                <p class="text-center text-n-700 text-xl font-medium leading-snug pt-2 md:pt-4">
                  {boxItem.subText}
                </p>
              )}
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
